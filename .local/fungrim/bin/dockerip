#!/bin/bash

if [ -z "$1" ]; then
	echo "Please supply a docker container ID or something to grep on"
	exit 1
fi

docker inspect $(docker ps | grep $1 | awk '{print $1}') | jq -r '.[0] | .NetworkSettings | .Networks | first(.[]) | .IPAddress'