#!/bin/bash

[ -z "$1" ] && echo "Missing profile name" && exit 1

if [ ! -f  ~/.ssh/config.$1 ]; then
	echo "Profile config does not exist: ~/.ssh/config.$1";
	exit 1;
fi

if [ -f ~/.ssh/config ]; then
    rm ~/.ssh/config
fi

cp ~/.ssh/config.$1 ~/.ssh/config
